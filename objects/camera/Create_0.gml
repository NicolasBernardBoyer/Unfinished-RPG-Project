/* cam = camera_create();
followPlayer = true;

var vm = matrix_build_lookat(x,y,-10,x,y,0,0,1,0);
var pm = matrix_build_projection_ortho(427,320,1,10000);

camera_set_view_mat(cam,vm);
camera_set_proj_mat(cam,pm);

view_camera[0] = cam;

follow = noone;

xTo = x;
yTo = y; */

following = obj_player;
h_border = 60;
v_border = 30;

